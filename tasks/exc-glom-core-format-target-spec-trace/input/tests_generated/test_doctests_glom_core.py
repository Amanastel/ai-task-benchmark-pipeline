"""Generated by okfpipe (stage 1) -- doctest materialisation.

Each test below runs the executable examples from one docstring and fails with
the real expected-vs-actual diff. These are *authored* expectations lifted out
of the source, so a failure here means documented behaviour changed.

Regenerate with:  ./run.sh <repo> --stages hygiene
Do not edit by hand -- edits are overwritten.
"""

import doctest
import importlib

import pytest

OPTIONFLAGS = doctest.NORMALIZE_WHITESPACE | doctest.IGNORE_EXCEPTION_DETAIL | doctest.ELLIPSIS


def _run(module_name, object_name):
    module = importlib.import_module(module_name)
    target = module
    for part in object_name.split("."):
        if part:
            target = getattr(target, part)
    wanted = f"{module_name}.{object_name}" if object_name else module_name

    # recurse=False keeps each generated test scoped to exactly one docstring,
    # so a failure names the function whose documented behaviour changed rather
    # than the whole module.
    finder = doctest.DocTestFinder(exclude_empty=True, recurse=False)
    tests = [t for t in finder.find(target, wanted, module=module) if t.examples]
    if not tests:
        pytest.skip(f"no doctest examples found for {wanted}")

    runner = doctest.DocTestRunner(optionflags=OPTIONFLAGS, verbose=False)
    output = []
    for test in tests:
        runner.run(test, out=output.append, clear_globs=True)
    if runner.failures:
        raise AssertionError(
            f"{runner.failures} of {runner.tries} doctest example(s) failed "
            f"for {wanted}:\n" + "".join(output)
        )


def test_doctest_Call():
    """1 documented example(s) for glom.core.Call."""
    _run("glom.core", "Call")


def test_doctest_Coalesce():
    """3 documented example(s) for glom.core.Coalesce."""
    _run("glom.core", "Coalesce")


def test_doctest_CoalesceError():
    """1 documented example(s) for glom.core.CoalesceError."""
    _run("glom.core", "CoalesceError")


def test_doctest_Fill():
    """1 documented example(s) for glom.core.Fill."""
    _run("glom.core", "Fill")


def test_doctest_Glommer():
    """2 documented example(s) for glom.core.Glommer."""
    _run("glom.core", "Glommer")


def test_doctest_Inspect():
    """1 documented example(s) for glom.core.Inspect."""
    _run("glom.core", "Inspect")


def test_doctest_Invoke():
    """4 documented example(s) for glom.core.Invoke."""
    _run("glom.core", "Invoke")


def test_doctest_Invoke_constants():
    """3 documented example(s) for glom.core.Invoke.constants."""
    _run("glom.core", "Invoke.constants")


def test_doctest_Invoke_specfunc():
    """1 documented example(s) for glom.core.Invoke.specfunc."""
    _run("glom.core", "Invoke.specfunc")


def test_doctest_Invoke_specs():
    """3 documented example(s) for glom.core.Invoke.specs."""
    _run("glom.core", "Invoke.specs")


def test_doctest_Invoke_star():
    """1 documented example(s) for glom.core.Invoke.star."""
    _run("glom.core", "Invoke.star")


def test_doctest_Let():
    """1 documented example(s) for glom.core.Let."""
    _run("glom.core", "Let")


def test_doctest_Path():
    """6 documented example(s) for glom.core.Path."""
    _run("glom.core", "Path")


def test_doctest_Path_from_text():
    """1 documented example(s) for glom.core.Path.from_text."""
    _run("glom.core", "Path.from_text")


def test_doctest_Path_items():
    """1 documented example(s) for glom.core.Path.items."""
    _run("glom.core", "Path.items")


def test_doctest_Path_values():
    """1 documented example(s) for glom.core.Path.values."""
    _run("glom.core", "Path.values")


def test_doctest_PathAccessError():
    """1 documented example(s) for glom.core.PathAccessError."""
    _run("glom.core", "PathAccessError")


def test_doctest_PathAssignError():
    """1 documented example(s) for glom.core.PathAssignError."""
    _run("glom.core", "PathAssignError")


def test_doctest_Pipe():
    """1 documented example(s) for glom.core.Pipe."""
    _run("glom.core", "Pipe")


def test_doctest_TType():
    """4 documented example(s) for glom.core.TType."""
    _run("glom.core", "TType")


def test_doctest_UnregisteredTarget():
    """1 documented example(s) for glom.core.UnregisteredTarget."""
    _run("glom.core", "UnregisteredTarget")


def test_doctest_format_invocation():
    """3 documented example(s) for glom.core.format_invocation."""
    _run("glom.core", "format_invocation")
