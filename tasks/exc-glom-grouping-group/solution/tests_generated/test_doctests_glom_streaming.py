"""Generated by okfpipe (stage 1) -- doctest materialisation.

Each test below runs the executable examples from one docstring and fails with
the real expected-vs-actual diff. These are *authored* expectations lifted out
of the source, so a failure here means documented behaviour changed.

Regenerate with:  ./run.sh <repo> --stages hygiene
Do not edit by hand -- edits are overwritten.
"""

import doctest
import importlib

import pytest

OPTIONFLAGS = doctest.NORMALIZE_WHITESPACE | doctest.IGNORE_EXCEPTION_DETAIL | doctest.ELLIPSIS


def _run(module_name, object_name):
    module = importlib.import_module(module_name)
    target = module
    for part in object_name.split("."):
        if part:
            target = getattr(target, part)
    wanted = f"{module_name}.{object_name}" if object_name else module_name

    # recurse=False keeps each generated test scoped to exactly one docstring,
    # so a failure names the function whose documented behaviour changed rather
    # than the whole module.
    finder = doctest.DocTestFinder(exclude_empty=True, recurse=False)
    tests = [t for t in finder.find(target, wanted, module=module) if t.examples]
    if not tests:
        pytest.skip(f"no doctest examples found for {wanted}")

    runner = doctest.DocTestRunner(optionflags=OPTIONFLAGS, verbose=False)
    output = []
    for test in tests:
        runner.run(test, out=output.append, clear_globs=True)
    if runner.failures:
        raise AssertionError(
            f"{runner.failures} of {runner.tries} doctest example(s) failed "
            f"for {wanted}:\n" + "".join(output)
        )


def test_doctest_First():
    """2 documented example(s) for glom.streaming.First."""
    _run("glom.streaming", "First")


def test_doctest_Iter():
    """2 documented example(s) for glom.streaming.Iter."""
    _run("glom.streaming", "Iter")


def test_doctest_Iter_all():
    """1 documented example(s) for glom.streaming.Iter.all."""
    _run("glom.streaming", "Iter.all")


def test_doctest_Iter_chunked():
    """2 documented example(s) for glom.streaming.Iter.chunked."""
    _run("glom.streaming", "Iter.chunked")


def test_doctest_Iter_dropwhile():
    """1 documented example(s) for glom.streaming.Iter.dropwhile."""
    _run("glom.streaming", "Iter.dropwhile")


def test_doctest_Iter_filter():
    """2 documented example(s) for glom.streaming.Iter.filter."""
    _run("glom.streaming", "Iter.filter")


def test_doctest_Iter_first():
    """1 documented example(s) for glom.streaming.Iter.first."""
    _run("glom.streaming", "Iter.first")


def test_doctest_Iter_flatten():
    """1 documented example(s) for glom.streaming.Iter.flatten."""
    _run("glom.streaming", "Iter.flatten")


def test_doctest_Iter_map():
    """2 documented example(s) for glom.streaming.Iter.map."""
    _run("glom.streaming", "Iter.map")


def test_doctest_Iter_slice():
    """2 documented example(s) for glom.streaming.Iter.slice."""
    _run("glom.streaming", "Iter.slice")


def test_doctest_Iter_split():
    """3 documented example(s) for glom.streaming.Iter.split."""
    _run("glom.streaming", "Iter.split")


def test_doctest_Iter_takewhile():
    """1 documented example(s) for glom.streaming.Iter.takewhile."""
    _run("glom.streaming", "Iter.takewhile")


def test_doctest_Iter_unique():
    """1 documented example(s) for glom.streaming.Iter.unique."""
    _run("glom.streaming", "Iter.unique")


def test_doctest_Iter_windowed():
    """1 documented example(s) for glom.streaming.Iter.windowed."""
    _run("glom.streaming", "Iter.windowed")
