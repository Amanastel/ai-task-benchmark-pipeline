# Generated by okfpipe (stage 1).
#
#   docker compose run --rm tests     # build if needed, then run the suite
#
services:
  tests:
    build:
      context: .
      dockerfile: Dockerfile
    image: okf-tomlkit:latest
    environment:
      PYTHONHASHSEED: "0"
      TZ: UTC
    network_mode: none
    command: ["./run-tests.sh"]
